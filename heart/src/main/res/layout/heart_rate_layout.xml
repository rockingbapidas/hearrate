<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data class="com.vantagecircle.heartrate.databinding.HeartRateBinding">

        <import type="android.view.View" />

        <variable
            name="heartFragmentHandler"
            type="com.vantagecircle.heartrate.fragment.handlers.HeartFragmentHandlers" />

        <variable
            name="heart"
            type="com.vantagecircle.heartrate.data.HeartM" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/fingerStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/finger_status"
            android:textColor="@android:color/holo_red_dark"
            android:textSize="20sp"
            android:visibility="@{heart.detectHeartRate ? View.GONE : View.VISIBLE}"
            android:layout_marginBottom="10dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/heartRate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{heart.beatsPerMinuteValue == null ? @string/blank : heart.beatsPerMinuteValue}"
            android:textSize="24sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/btnStart"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:onClick="@{(view) -> heartFragmentHandler.onStartClick()}"
            android:text="@{heart.started ? @string/stop : @string/start}" />

    </LinearLayout>
</layout>